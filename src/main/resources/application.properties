server.port=${PORT:8080}

logging.level.org.atmosphere=warn
spring.mustache.check-template-location=false

# Launch the default browser when starting the application in development mode
vaadin.launch-browser=true

# ✅ MySQL configuration (Replace PostgreSQL section)
spring.datasource.url=jdbc:mysql://localhost:3306/hris?serverTimezone=UTC
spring.datasource.username=fireclay
spring.datasource.password=Password1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Vaadin configuration
vaadin.allowed-packages=com.vaadin,org.vaadin,com.flowingcode,com.vincent.hris
